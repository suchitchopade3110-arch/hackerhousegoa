import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // No server-side image generation, no rewrites, no auth middleware.
  // Keep this file empty on purpose — see BACKEND_PRD §2.3 (out of scope).
}

export default nextConfig
