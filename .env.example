# Absolute origin, no trailing slash. NEVER localhost in production —
# lib/env.ts throws at module load if it is. See BACKEND_PRD §6.2.
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Vercel Blob read/write token. Server-only — do not prefix with NEXT_PUBLIC_.
BLOB_READ_WRITE_TOKEN=
